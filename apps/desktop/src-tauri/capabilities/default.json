{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for the CODETAS management window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
